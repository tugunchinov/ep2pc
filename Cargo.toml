workspace = { members = ["discovery", "peer", "repo"] }
[package]
name = "peer-communication"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
